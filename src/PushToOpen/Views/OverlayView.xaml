<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PushToOpen.Views.OverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="using:PushToOpen.Controls">
    <Grid x:Name="OverlayRoot" Background="#CC0E1014">

        <Border CornerRadius="14" BorderBrush="{ThemeResource DividerBrush}" BorderThickness="1" Padding="14" Margin="2">
            <StackPanel Spacing="10">
                <Grid>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Ellipse Width="8" Height="8"
                                 Fill="{x:Bind ViewModel.IsGateOpen, Mode=OneWay, Converter={StaticResource GateBrush}}" />
                        <TextBlock Text="PushToOpen" Foreground="{ThemeResource TextPrimaryBrush}"
                                   FontWeight="SemiBold" FontSize="12" />
                    </StackPanel>
                    <TextBlock HorizontalAlignment="Right" Style="{StaticResource LabelText}"
                               Text="{x:Bind ViewModel.IsGateOpen, Mode=OneWay, Converter={StaticResource GateLabel}}" />
                </Grid>
                <ctrl:VuMeter Level="{x:Bind ViewModel.RmsNormalized, Mode=OneWay}"
                              Threshold="{x:Bind ViewModel.ThresholdNormalized, Mode=OneWay}"
                              IsActive="{x:Bind ViewModel.IsGateOpen, Mode=OneWay}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
